// Public API for timeline page
// Following FSD principles - expose only what's needed from outside

export { TimelinePageContent as TimelinePage } from './ui';
export { useTimelineData } from './model';
